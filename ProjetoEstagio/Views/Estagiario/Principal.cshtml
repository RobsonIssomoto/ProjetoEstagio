@model ProjetoEstagio.Models.EstagiarioModel
@{
    Layout = "_LayoutEstagiario";
    ViewData["Title"] = "Principal Estagiário";
}

<style>
    /* Estilo dos cards do dashboard (que você já tinha) */
    .dashboard-card {
        transition: all 0.2s ease-in-out;
    }

        .dashboard-card:hover {
            transform: translateY(-5px);
            filter: brightness(1.1);
        }

    /* --- LÓGICA DO STEPPER CORRIGIDA --- */
    .stepper-wrapper {
        display: flex;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    .stepper-item {
        position: relative;
        display: flex;
        flex-direction: column;
        align-items: center;
        flex: 1;
    }

        /* --- LINHA (::after) --- */
        /* Desenha a linha DEPOIS da bolha */
        .stepper-item::after {
            content: "";
            position: absolute;
            top: 12px; /* Alinha no centro da bolha */
            left: 50%; /* Começa no centro da bolha */
            width: 100%; /* Vai até a próxima bolha */
            height: 3px;
            background-color: #e0e0e0; /* Cinza padrão */
            z-index: 1; /* Fica atrás da bolha */
        }

        /* Remove a linha do ÚLTIMO item */
        .stepper-item:last-child::after {
            content: none;
        }

    /* --- BOLHA (Dot) --- */
    .stepper-dot {
        width: 24px;
        height: 24px;
        border-radius: 50%;
        background-color: #e0e0e0; /* Cinza padrão */
        z-index: 2; /* Fica na frente da linha */
        transition: all 0.3s ease;
    }

    /* --- RÓTULO (Label) --- */
    .stepper-label {
        text-align: center;
        margin-top: 0.75rem;
        font-size: 0.9rem;
        color: #6c757d;
        font-weight: 500;
    }

    /* --- CLASSES DINÂMICAS --- */

    /* Etapa Concluída (Verde) */
    .stepper-item.completed .stepper-dot {
        background-color: var(--bs-success);
    }

    .stepper-item.completed .stepper-label {
        color: var(--bs-success);
        font-weight: 500; /* Alterado para peso normal */
    }
    /* A LINHA *DEPOIS* de um item completo fica verde */
    .stepper-item.completed::after {
        background-color: var(--bs-success);
    }

    /* Etapa Ativa (Verde) */
    .stepper-item.active .stepper-dot {
        background-color: var(--bs-success);
        transform: scale(1.2);
    }

    .stepper-item.active .stepper-label {
        color: var(--bs-success);
        font-weight: 700;
    }
</style>

<div class="card shadow px-4 pb-4">
    <div class="card-header bg-white border-0 p-4">
        <h5 class="mb-0 text-center fw-bold">Minhas Pendências</h5>
    </div>

<div class="row g-3">
    <div class="col-12 col-md-4">
        <div class="card text-bg-primary h-100 dashboard-card shadow-sm">
            <div class="card-body pb-0">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="fs-1 fw-bold">1</div>
                        <h5 class="mb-0">Processo Pendente</h5>
                    </div>
                    <i class="fas fa-file-signature fs-1 opacity-50"></i>
                </div>
            </div>
            <div class="card-footer bg-transparent border-top-0 pt-0">
                <a href="#" class="stretched-link text-white text-decoration-none fw-semibold">
                    Ver pendências
                    <i class="fas fa-arrow-circle-right ms-1 small"></i>
                </a>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-4">
        <div class="card text-bg-success h-100 dashboard-card shadow-sm">
            <div class="card-body pb-0">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="fs-1 fw-bold">2</div>
                        <h5 class="mb-0">Relatórios Pendentes</h5>
                    </div>
                    <i class="fas fa-file-alt fs-1 opacity-50"></i>
                </div>
            </div>
            <div class="card-footer bg-transparent border-top-0 pt-0">
                <a href="#" class="stretched-link text-white text-decoration-none fw-semibold">
                    Ver relatórios
                    <i class="fas fa-arrow-circle-right ms-1 small"></i>
                </a>
            </div>
        </div>
    </div>

    <div class="col-12 col-md-4">
        <div class="card text-bg-warning h-100 dashboard-card shadow-sm">
            <div class="card-body pb-0">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <div class="fs-1 fw-bold">3</div>
                        <h5 class="mb-0">Documentos Pendentes</h5>
                    </div>
                    <i class="fas fa-folder-open fs-1 opacity-50"></i>
                </div>
            </div>
            <div class="card-footer bg-transparent border-top-0 pt-0">
                <a href="#" class="stretched-link text-dark text-decoration-none fw-semibold">
                    Ver documentos
                    <i class="fas fa-arrow-circle-right ms-1 small"></i>
                </a>
            </div>
        </div>
    </div>
</div>
</div>
<div class="card shadow my-4">
    <div class="card-header bg-white border-0 p-4">
        <h5 class="mb-0 text-center fw-bold">Visão Geral do Estágio</h5>
        <p class="text-center text-muted mb-4">Gerencie seu pedido de estágio e monitore o andamento dele.</p>
    </div>

    <div class="card-body p-4 p-md-5">

        <h6 class="fw-semibold mb-4 text-center">Status Atual do Processo</h6>

        <div class="stepper-wrapper">
            <div class="stepper-item">
                <div class="stepper-dot"></div>
                <div class="stepper-label">Processo a Iniciar</div>
            </div>
            <div class="stepper-item">
                <div class="stepper-dot"></div>
                <div class="stepper-label">Em Análise</div>
            </div>
            <div class="stepper-item">
                <div class="stepper-dot"></div>
                <div class="stepper-label">Doc. Pendentes</div>
            </div>
            <div class="stepper-item">
                <div class="stepper-dot"></div>
                <div class="stepper-label">Estágio Ativo</div>
            </div>
        </div>

        <div class="d-flex py-3 ">
            <a asp-controller="Estagiario" asp-action="Processo" class="btn btn-success shadow-sm py-3 px-5">
                <i class="fas fa-plus-circle me-2"></i> Iniciar Processo de Estágio
            </a>
        </div>


    </div>
</div>